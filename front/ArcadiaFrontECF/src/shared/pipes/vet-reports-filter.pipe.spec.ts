import { VetReportsFilterPipe } from './vet-reports-filter.pipe';

describe('VetReportsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VetReportsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
